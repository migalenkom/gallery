<div>

  <% @galleries.each do |gallery| %>

    <%= image_tag gallery.cover.name.thumb.url %>
    <h4><%=link_to "#{gallery.name}", image_gallery_images_path(gallery)%></h4>

  <% end %>

  <%= paginate @galleries %>

</div>